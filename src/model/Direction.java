package model;

/**
 * Represents the possible directions for certain operations or entities.
 */
public enum Direction {
    HORIZONTAL,     // Represents horizontal direction (e.g., along the x or z axis)
    VERTICAL        // Represents vertical direction (e.g., along the y axis)
}
